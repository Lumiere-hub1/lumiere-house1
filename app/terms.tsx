/**
 * /terms — the canonical URL given to third-party reviewers, which expect
 * terms at the site root. Re-export of app/legal/terms.tsx; see app/privacy.tsx
 * for why this is a re-export and not a second copy.
 */
export { default } from "./legal/terms";
